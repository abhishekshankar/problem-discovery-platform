# Copy to .env in this directory (src/problem_discovery/.env) for local dev.
# Repo-root .env (parent of src/) also works and overrides these values — see config.py

DATABASE_URL=postgresql://signal:signal@localhost:5433/signal

# Optional
# ANTHROPIC_API_KEY=
# ARCHIVE_LOCAL_DIR=../../data/signal_archive
# SIGNAL_EXTRACTION_STUB=1
